import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lazerwalker.walk-the-world',
  appName: 'Walk The World',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
